class DbConnection

  constructor(@config) ->

  openDatabase: ->
    @db =

  closeDatabase: ->

  createDatabase: ->

  createCollection: (name) ->

  createCollection: (name) ->

  createDocument: (document) ->

  createCollections: ->
    @poCollection =
    @parentChildCollection =

  createPo: ->
